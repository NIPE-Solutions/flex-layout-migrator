# Changesets

Add a changeset for user-facing behavior, CLI, or API changes. Tests, internal refactors, maintenance, and documentation-only pull requests do not require one.

Run `npm run changeset`, select the package and release impact, and write a concise user-facing summary. Publishing remains disabled until the v2 beta release workflow is reviewed separately.
