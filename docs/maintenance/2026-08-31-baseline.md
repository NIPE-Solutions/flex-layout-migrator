# Repository health baseline

Recorded on 31 August 2026 before the v2 repository-foundation work.

## Environment

- Node.js: 20.12.2
- npm: 10.5.0
- Installed from the committed lockfile with `npm ci`

## Results

| Command                   | Result | Summary                                                                                                |
| ------------------------- | ------ | ------------------------------------------------------------------------------------------------------ |
| `npm run build`           | Failed | TypeScript could not resolve Prettier declarations.                                                    |
| `npm test -- --runInBand` | Failed | 25 suites passed and 9 failed; Jest also discovered emitted tests under `dist`.                        |
| `npm run lint`            | Failed | The legacy ESLint configuration could not resolve its plugin unambiguously across the linked worktree. |
| `npm run prettier`        | Failed | Three source files did not match the configured format.                                                |
| `npm audit`               | Failed | 20 advisories: 3 low, 6 moderate, 8 high, and 3 critical.                                              |

This file records the starting state. It is not a support or compatibility statement.
